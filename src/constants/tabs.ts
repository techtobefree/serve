export const TAB_SELECTION_HEIGHT = '16'

// Dynamic tailwind isn't picked up, so this is explicit
export const uses = [
  'h-16'
]

export const TAB_PATH = {
  home: '/',
  projects: '/projects',
  groups: '/groups',
  messages: '/messages',
}
